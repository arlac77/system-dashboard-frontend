export class NetworkInterface {
  constructor(json) {
    Object.assign(this, json);
  }
}

export class Named {
  constructor(json) {
    Object.assign(this, json);
  }
}

export class Fail2Ban {
  constructor(json) {
    Object.assign(this, json);
  }
}
